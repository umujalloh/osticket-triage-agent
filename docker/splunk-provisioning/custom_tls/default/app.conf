[install]
is_configured = 0

[ui]
is_visible = 0
label = Custom TLS

[launcher]
description = Points Splunk's management and HEC listeners at a certificate unique to this deployment instead of the shipped default.
